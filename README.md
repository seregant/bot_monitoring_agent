Server Monitoring API using NodeJs

How to use ?
1. Clone it
2. Setting your server key and port in `config.js`
3. Run `npm install`
4. Run `npm start`. Now your application is running. Try to access `/sysinfo`